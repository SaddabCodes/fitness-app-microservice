spring:
  application:
    name: UserService
  datasource:
    url: jdbc:mysql://localhost:3306/fitness_micro_user
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

server:
  port: 8081